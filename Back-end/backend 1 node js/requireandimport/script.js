import { sum, sub } from "./maths.js";
console.log(sum(1, 2));
console.log(sum(3, 10));

//either in a project we can only use imprt or required not both
//but if we are using imort then we have to first initialize package.jason
//inside package.jason after initilizing all we also have to write "type":"module"
