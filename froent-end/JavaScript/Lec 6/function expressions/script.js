//function expressions is a way to define a function in a variable. It is also called anonymous function because it does not have a name.

let sum = function (a, b) {
  return a + b;
};

console.log(sum(1, 2));
