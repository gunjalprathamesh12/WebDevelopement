let a=10;
let b=20;
let sum=a+b;
console.log(`The sum of ${a} and ${b} is ${sum}`);
// Output: The sum of 10 and 20 is 30

let pencile=10;
let eraser=5;
console.log(`The Price of Pencile is ${pencile} and the price of eraser is ${eraser}`);

let name="John";
let age=30;
console.log(`my name is ${name} and I am ${age} years old`);

//templete literals can also be used to create multi-line strings
let multiLineString=`This is a multi-line string.
It can span multiple lines.`;
console.log(multiLineString);

let exp=10;
let ss=90;
console.log("price of exp is"+exp+"and the price of ss is"+ss);
    